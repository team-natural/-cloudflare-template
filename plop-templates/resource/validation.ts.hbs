// Generated by the `scaffold` skill. Regenerating overwrites hand edits.
import { createInsertSchema, createUpdateSchema } from "drizzle-zod";
import type { z } from "zod";
import { {{table}} } from "@app/schema";

export const create{{pascalCase name}}Schema = createInsertSchema({{table}}).pick({
{{#each fieldsList}}
  {{this}}: true,
{{/each}}
});

export const update{{pascalCase name}}Schema = createUpdateSchema({{table}}).pick({
{{#each fieldsList}}
  {{this}}: true,
{{/each}}
});

export type Create{{pascalCase name}}Input = z.infer<typeof create{{pascalCase name}}Schema>;
export type Update{{pascalCase name}}Input = z.infer<typeof update{{pascalCase name}}Schema>;
