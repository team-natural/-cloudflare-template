---
// {{screenId}} {{screenName}} — PRD-04 §3-{{sectionNumber}}
// Skeleton only: route + layout + heading. Build the real screen with the
// `{{designSkill}}` skill, which owns every layout/composition decision here.
import Layout from "{{layoutImport}}";
---

<Layout title="{{screenName}}">
  <main>
    <h1>{{screenName}}</h1>
  </main>
</Layout>
